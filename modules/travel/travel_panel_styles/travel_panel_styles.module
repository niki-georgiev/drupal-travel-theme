<?php
/**
 * Implementation of hook_ctools_plugin_directory().
 */
function travel_panel_styles_ctools_plugin_directory($module, $plugin) {
  if ($module == 'panels' && !empty($plugin)) {
    return 'plugins/' . $plugin;
  }
}